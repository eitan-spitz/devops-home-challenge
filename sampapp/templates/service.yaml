apiVersion: v1
kind: Service
metadata:
  name: {{ include "sampapp.fullname" . }}
  labels:
    {{- include "sampapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sampapp.selectorLabels" . | nindent 4 }}
