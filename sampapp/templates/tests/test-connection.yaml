apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sampapp.fullname" . }}-test-connection"
  labels:
    {{- include "sampapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sampapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
